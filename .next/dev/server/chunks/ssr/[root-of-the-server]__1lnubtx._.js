module.exports = [
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[project]/app/HomeClient.tsx [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>Page
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-jsx-dev-runtime.js [app-rsc] (ecmascript)");
// Server Component (no "use client") — this is what lets us read
// nour.svg off disk with `fs` and pass the derived chunk list down as
// plain props, instead of hardcoding a copy in client code.
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/nour-path.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$app$2f$HomeClient$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/app/HomeClient.tsx [app-rsc] (ecmascript)");
;
;
;
function Page() {
    return /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$app$2f$HomeClient$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"], {
        nourChunks: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["NOUR_CHUNKS"],
        nourViewBox: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["NOUR_VIEWBOX"],
        nourStrokeWidth: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["NOUR_STROKE_WIDTH"]
    }, void 0, false, {
        fileName: "[project]/app/HomeClient.tsx",
        lineNumber: 9,
        columnNumber: 5
    }, this);
}
}),
"[project]/app/favicon.ico (static in ecmascript, tag client)", ((__turbopack_context__) => {

__turbopack_context__.v("/_next/static/media/favicon.2vob68tjqpejf.ico" + (globalThis["NEXT_CLIENT_ASSET_SUFFIX"] || ''));}),
"[project]/app/favicon.ico.mjs { IMAGE => \"[project]/app/favicon.ico (static in ecmascript, tag client)\" } [app-rsc] (structured image object, ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$app$2f$favicon$2e$ico__$28$static__in__ecmascript$2c$__tag__client$29$__ = __turbopack_context__.i("[project]/app/favicon.ico (static in ecmascript, tag client)");
;
const __TURBOPACK__default__export__ = {
    src: __TURBOPACK__imported__module__$5b$project$5d2f$app$2f$favicon$2e$ico__$28$static__in__ecmascript$2c$__tag__client$29$__["default"],
    width: 256,
    height: 256
};
}),
"[project]/app/page.tsx [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>Page
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-jsx-dev-runtime.js [app-rsc] (ecmascript)");
// Server Component (no "use client") — this is what lets us read
// nour.svg off disk with `fs` and pass the derived chunk list down as
// plain props, instead of hardcoding a copy in client code.
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/nour-path.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$app$2f$HomeClient$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/app/HomeClient.tsx [app-rsc] (ecmascript)");
;
;
;
function Page() {
    return /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$app$2f$HomeClient$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"], {
        nourChunks: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["NOUR_CHUNKS"],
        nourViewBox: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["NOUR_VIEWBOX"],
        nourStrokeWidth: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$nour$2d$path$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["NOUR_STROKE_WIDTH"]
    }, void 0, false, {
        fileName: "[project]/app/page.tsx",
        lineNumber: 9,
        columnNumber: 5
    }, this);
}
}),
"[project]/app/page.tsx [app-rsc] (ecmascript, Next.js Server Component)", (function(__turbopack_context__){

__turbopack_context__.n(__turbopack_context__.i("[project]/app/page.tsx [app-rsc] (ecmascript)"));
}),
"[project]/lib/nour-path.ts [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "NOUR_CHUNKS",
    ()=>NOUR_CHUNKS,
    "NOUR_PATH_D",
    ()=>NOUR_PATH_D,
    "NOUR_STROKE_WIDTH",
    ()=>NOUR_STROKE_WIDTH,
    "NOUR_VIEWBOX",
    ()=>NOUR_VIEWBOX
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$svgpath$2f$index$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/svgpath/index.js [app-rsc] (ecmascript)");
;
;
;
// ---- read + normalize -----------------------------------------------
function readNourSvgRaw() {
    const svgFilePath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), "public", "nour.svg");
    return __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].readFileSync(svgFilePath, "utf-8");
}
function attr(tag, name) {
    const m = tag.match(new RegExp(`${name}="([^"]*)"`));
    return m ? m[1] : null;
}
const svgContent = readNourSvgRaw();
const pathTagMatch = svgContent.match(/<path\b[^>]*>/);
if (!pathTagMatch) {
    throw new Error("nour.svg: no <path> element found.");
}
const pathTag = pathTagMatch[0];
const rawD = attr(pathTag, "d");
if (!rawD) {
    throw new Error("nour.svg: <path> has no d attribute.");
}
// This file's coordinates already have any transform baked in — but if
// you re-export from Inkscape and it adds one back, this still handles
// it rather than silently dropping it.
const rawTransform = attr(pathTag, "transform");
const rawStrokeWidth = attr(pathTag, "stroke-width");
let normalized = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$svgpath$2f$index$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"])(rawD).abs();
if (rawTransform) {
    const translateMatch = rawTransform.match(/translate\(\s*(-?[\d.]+)[ ,]+(-?[\d.]+)\s*\)/);
    if (translateMatch) {
        normalized = normalized.translate(parseFloat(translateMatch[1]), parseFloat(translateMatch[2]));
    }
}
normalized = normalized.unshort();
const fullD = normalized.round(4).toString();
// ---- split into individual strokes, in raw file order -------------------
function splitIntoStrokes(d) {
    const tokens = d.match(/[MC][^MC]*/g) ?? [];
    const strokes = [];
    let current = "";
    for (const tok of tokens){
        if (tok[0] === "M") {
            if (current) strokes.push(current.trim());
            current = tok;
        } else {
            current += " " + tok;
        }
    }
    if (current) strokes.push(current.trim());
    return strokes;
}
function parseSegments(d) {
    const tokens = d.match(/[MCL][^MCL]*/g) ?? [];
    const nums = (s)=>(s.match(/-?\d*\.?\d+(?:[eE][-+]?\d+)?/g) ?? []).map(Number);
    let start = {
        x: 0,
        y: 0
    };
    let cursor = {
        x: 0,
        y: 0
    };
    const segments = [];
    for (const tok of tokens){
        const cmd = tok[0];
        const n = nums(tok.slice(1));
        if (cmd === "M") {
            cursor = {
                x: n[0],
                y: n[1]
            };
            start = {
                ...cursor
            };
        } else if (cmd === "C") {
            for(let i = 0; i + 5 < n.length; i += 6){
                const to = {
                    x: n[i + 4],
                    y: n[i + 5]
                };
                segments.push({
                    cmd: "C",
                    cp1: {
                        x: n[i],
                        y: n[i + 1]
                    },
                    cp2: {
                        x: n[i + 2],
                        y: n[i + 3]
                    },
                    to
                });
                cursor = to;
            }
        } else if (cmd === "L") {
            for(let i = 0; i + 1 < n.length; i += 2){
                const to = {
                    x: n[i],
                    y: n[i + 1]
                };
                segments.push({
                    cmd: "L",
                    to
                });
                cursor = to;
            }
        }
    }
    return {
        start,
        end: cursor,
        segments
    };
}
function cubicPoint(p0, p1, p2, p3, t) {
    const mt = 1 - t;
    return {
        x: mt * mt * mt * p0.x + 3 * mt * mt * t * p1.x + 3 * mt * t * t * p2.x + t * t * t * p3.x,
        y: mt * mt * mt * p0.y + 3 * mt * mt * t * p1.y + 3 * mt * t * t * p2.y + t * t * t * p3.y
    };
}
function strokeLength(d) {
    const { start, segments } = parseSegments(d);
    let cursor = start;
    let len = 0;
    for (const seg of segments){
        if (seg.cmd === "L") {
            len += Math.hypot(seg.to.x - cursor.x, seg.to.y - cursor.y);
            cursor = seg.to;
        } else {
            const p0 = cursor;
            const samples = 20;
            let prev = p0;
            for(let s = 1; s <= samples; s++){
                const pt = cubicPoint(p0, seg.cp1, seg.cp2, seg.to, s / samples);
                len += Math.hypot(pt.x - prev.x, pt.y - prev.y);
                prev = pt;
            }
            cursor = seg.to;
        }
    }
    return len;
}
function fmt(n) {
    return String(Math.round(n * 10000) / 10000);
}
function buildD(start, segments) {
    let d = `M${fmt(start.x)} ${fmt(start.y)}`;
    for (const seg of segments){
        if (seg.cmd === "L") {
            d += ` L${fmt(seg.to.x)} ${fmt(seg.to.y)}`;
        } else {
            d += ` C${fmt(seg.cp1.x)} ${fmt(seg.cp1.y)} ${fmt(seg.cp2.x)} ${fmt(seg.cp2.y)} ${fmt(seg.to.x)} ${fmt(seg.to.y)}`;
        }
    }
    return d;
}
// Reverses a single M/C/L stroke so it draws tail-first instead of
// head-first (used for the couple of strokes in DRAW_ORDER below that
// connect more smoothly that way).
function reverseStrokeD(d) {
    const { start, segments } = parseSegments(d);
    const anchors = [
        start,
        ...segments.map((s)=>s.to)
    ];
    const revAnchors = anchors.slice().reverse();
    const revSegments = [];
    for(let i = segments.length - 1; i >= 0; i--){
        const seg = segments[i];
        const to = revAnchors[segments.length - i];
        if (seg.cmd === "L") {
            revSegments.push({
                cmd: "L",
                to
            });
        } else {
            revSegments.push({
                cmd: "C",
                cp1: seg.cp2,
                cp2: seg.cp1,
                to
            });
        }
    }
    return buildD(revAnchors[0], revSegments);
}
function dist(a, b) {
    return Math.hypot(a.x - b.x, a.y - b.y);
}
// ---- draw order: N -> O -> U -> R, left to right, no backtracking -------
//
// Index = position in the raw file (0-8, in the order `splitIntoStrokes`
// produces them for the current nour.svg). Verified by rendering each
// stroke in a distinct color and matching it to the letter it draws:
//   N: 8
//   O: 0 (entry hook), 7 (main loop), 1 (exit, drawn in reverse)
//   U: 6 (first hump), 2 (second hump)
//   R: 5 (stem, drawn in reverse), 4 (top loop), 3 (tail flourish)
const DRAW_ORDER = [
    {
        index: 8
    },
    {
        index: 0
    },
    {
        index: 7
    },
    {
        index: 1,
        reversed: true
    },
    {
        index: 6
    },
    {
        index: 2
    },
    {
        index: 5,
        reversed: true
    },
    {
        index: 4
    },
    {
        index: 3
    }
];
const rawStrokeDs = splitIntoStrokes(fullD);
if (rawStrokeDs.length !== DRAW_ORDER.length) {
    throw new Error(`nour.svg: expected ${DRAW_ORDER.length} strokes but found ${rawStrokeDs.length}. ` + `The hand-verified DRAW_ORDER in nour-path.ts no longer matches the file and needs ` + `to be re-derived (map each stroke to the letter it draws, then re-sequence).`);
}
const orderedStrokeDs = DRAW_ORDER.map(({ index, reversed })=>reversed ? reverseStrokeD(rawStrokeDs[index]) : rawStrokeDs[index]);
// GAP_EPSILON: below this distance, two strokes are treated as already
// touching, so no retrace/jump chunks are inserted between them.
const GAP_EPSILON = 0.01;
// Build the ordered chunk list: for each stroke, a visible forward-draw
// chunk, then — only where a real gap follows it — a visible retrace
// chunk (the same stroke, reversed, going back over ink already on
// screen) and an invisible jump chunk (opacity permanently 0) straight
// to the next stroke's real start point. See point 2 in the header
// comment for why this shape specifically.
function buildChunks(strokeDs) {
    const chunks = [];
    for(let i = 0; i < strokeDs.length; i++){
        const d = strokeDs[i];
        const { start, end } = parseSegments(d);
        const len = strokeLength(d);
        // 1. Forward draw — the stroke exactly as it is in nour.svg.
        chunks.push({
            d,
            length: len,
            visible: true
        });
        if (i === strokeDs.length - 1) continue; // last stroke, nothing follows
        const nextStart = parseSegments(strokeDs[i + 1]).start;
        if (dist(end, nextStart) <= GAP_EPSILON) continue; // strokes already touch
        // 2. Retrace — same stroke, walked backward from `end` to `start`.
        // Visually this re-inks pixels that are already on screen, so it
        // never looks like new drawing; it just reads as the pen backing up.
        const retraceD = reverseStrokeD(d);
        chunks.push({
            d: retraceD,
            length: len,
            visible: true
        });
        // 3. Invisible jump — from this stroke's own start (where the
        // retrace just ended) to the next stroke's real start. Always
        // rendered at opacity 0, so however its timing lines up relative to
        // its neighbors, nothing is ever visibly drawn across the gap.
        //
        // NOTE: jumpDist is measured from `start`, not `end` — after the
        // retrace, that's where the pen actually is. `start` can legitimately
        // sit at (or extremely near) `nextStart` even when `end` doesn't, so
        // this needs its own epsilon check. Skipping it when near-zero isn't
        // just an optimization: a zero-length "d" (M and L to the same point)
        // has a real getTotalLength() of 0, and animating pathLength on a
        // zero-length path with framer-motion produces NaN internally, which
        // was the actual cause of the "Maximum call stack size exceeded"
        // crash — Framer's animation loop never converges on NaN and spins.
        const jumpDist = dist(start, nextStart);
        if (jumpDist > GAP_EPSILON) {
            const jumpD = buildD(start, [
                {
                    cmd: "L",
                    to: nextStart
                }
            ]);
            chunks.push({
                d: jumpD,
                length: jumpDist,
                visible: false
            });
        }
    }
    return chunks.filter((c)=>c.length > GAP_EPSILON);
}
const NOUR_CHUNKS = buildChunks(orderedStrokeDs);
// ---- tight viewBox, derived from the actual artwork ----------------------
//
// The <svg> tag's own viewBox is often just the export canvas (e.g. a
// full A4 page), not the artwork's bounds — that was silently making the
// boot animation draw a small glyph inside a mostly-empty frame. Compute
// the real bounding box from every point in the path instead, with a
// little padding for the stroke width.
const PADDING = 6;
function computeTightViewBox(d, strokeWidth) {
    const nums = (s)=>(s.match(/-?\d*\.?\d+(?:[eE][-+]?\d+)?/g) ?? []).map(Number);
    const tokens = d.match(/[MCL][^MCL]*/g) ?? [];
    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    for (const tok of tokens){
        const n = nums(tok.slice(1));
        for(let i = 0; i + 1 < n.length; i += 2){
            minX = Math.min(minX, n[i]);
            maxX = Math.max(maxX, n[i]);
            minY = Math.min(minY, n[i + 1]);
            maxY = Math.max(maxY, n[i + 1]);
        }
    }
    const pad = PADDING + strokeWidth;
    minX -= pad;
    minY -= pad;
    maxX += pad;
    maxY += pad;
    return `${fmt(minX)} ${fmt(minY)} ${fmt(maxX - minX)} ${fmt(maxY - minY)}`;
}
const NOUR_STROKE_WIDTH = rawStrokeWidth ? parseFloat(rawStrokeWidth) : 1.265;
const NOUR_VIEWBOX = computeTightViewBox(fullD, NOUR_STROKE_WIDTH);
const NOUR_PATH_D = fullD;
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__1lnubtx._.js.map